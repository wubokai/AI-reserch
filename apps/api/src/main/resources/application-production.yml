app:
  data-mode: ${DATA_MODE:REAL}
  worker:
    enabled: ${RESEARCH_WORKER_ENABLED:true}
  security:
    demo-principal:
      enabled: false
      username: ""
      password: ""

management:
  endpoint:
    health:
      show-details: never
